{
  "name": "local-api-checker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local OpenAI-compatible endpoint and API key checker, no dependencies",
  "scripts": { "start": "node server.mjs", "test": "node --test" },
  "engines": { "node": ">=22" }
}

